interface:
  display_name: "EmberDesk Frontend Slice"
  short_description: "Modify jQuery UI without breaking compat."
  default_prompt: "Use this when changing EmberDesk public UI, page controllers, shared browser libraries, extension surfaces, slash commands, macros, or compatibility-sensitive frontend behavior."
