interface:
  display_name: "EmberDesk Backend API Data"
  short_description: "Work on Express APIs, file-backed data, and derived caches."
  default_prompt: "Use this when changing EmberDesk server routes, user storage, character/chat/world-info APIs, secrets, migrations, tokenizers, vectors, or derived caches."
