interface:
  display_name: "EmberDesk Security Integrations"
  short_description: "Handle auth, secrets, proxies, and providers."
  default_prompt: "Use this when changing EmberDesk auth, sessions, CSRF, whitelists, Host checks, SSRF filtering, secrets, CORS/request proxy, server plugins, or external provider integrations."
