## Review

- [x] Review: Fallback streaming responses were parsed with the primary provider parser instead of the fallback OpenAI-compatible parser (severity: high; scope: chat generation lifecycle; evidence: `public/scripts/openai.js`; proof: red `bun run --cwd tests test:e2e -- chat-message-streaming.e2e.js -g "parses fallback stream with fallback source"`, green same command, green full `chat-message-streaming.e2e.js`)
- [x] Review: Recovered `continue`/`swipe` replacement could reset swipe history, and failed overswipe recovery could restore an out-of-range `swipe_id` before the swipe wrapper re-rendered the row (severity: high; scope: chat generation lifecycle; evidence: `public/script.js`, `public/scripts/chat-generation-lifecycle.js`; proof: red/green `bun run --cwd tests test:unit -- chat-generation-lifecycle.test.js --runInBand`, green full `chat-message-streaming.e2e.js`)
- [x] Review: Recovered overswipe retries restored the display baseline but not the intended append swipe slot before the next attempt, so fallback success could overwrite an existing swipe instead of appending (severity: high; scope: chat generation lifecycle; evidence: `public/script.js`, `public/scripts/chat-generation-lifecycle.js`; proof: red/green `bun run --cwd tests test:unit -- chat-generation-lifecycle.test.js --runInBand`, green `bun run --cwd tests test:e2e -- chat-message-streaming.e2e.js -g "recovered overswipe appends"`)
- [x] Review: Recovered non-streaming `continue` replacement overwrote existing reasoning state instead of matching the normal `appendFinal` path that preserves prior reasoning (severity: medium; scope: chat generation lifecycle; evidence: `public/script.js`, `public/scripts/chat-generation-lifecycle.js`; proof: red/green `bun run --cwd tests test:unit -- chat-generation-lifecycle.test.js --runInBand`)
- [x] Review: External content provider host classification accepted provider-looking superdomains through broad `host.includes(...)` checks (severity: medium; scope: external import/security; evidence: `src/endpoints/external-content-import-service.js`; proof: red/green `bun run --cwd tests test:unit -- external-content-import-service.test.js --runInBand`)
- [x] Review: Character rename write-failure side-effect order and route response shape lacked focused regression proof after command delegation (severity: low; scope: character card write command; evidence: `src/endpoints/character-write-service.js`, `src/endpoints/characters.js`; proof: green `bun run --cwd tests test:unit -- character-route-source-contract.test.js character-write-service.test.js --runInBand`)
- [x] Review: Dead `getGenerationAutoRecoveryAttempts()` marker function kept root lint red after lifecycle extraction (severity: low; scope: frontend structure contract; evidence: `public/script.js`, `tests/chat-workspace-structure.test.js`; proof: green `bun run lint`, green `bun run --cwd tests test:unit -- chat-workspace-structure.test.js --runInBand`)
- [x] Doc ID gate: `bun run docs:check` and `bun run docs:build` passed; generated topology had no degree-0 orphan nodes.
- [x] Claude candidate review unavailable: CLI run exceeded the configured budget before producing a report; temporary `.stream.jsonl` was deleted and no Claude candidate findings were used.
- [x] Final validation after review fixes: `bun run lint`, focused unit batch, full `bun run --cwd tests test:e2e -- chat-message-streaming.e2e.js`, `bun run test:compat`, and `git diff --check` passed.
- [x] Tests lint note: `bun run --cwd tests lint` still fails on existing source-marker quote errors in `tests/chat-workspace-structure.test.js`, `tests/openai-provider-capabilities.test.js`, and `tests/secrets-input-map.test.js`, plus existing Playwright `not.toBeVisible()` warnings in `tests/chat-message-streaming.e2e.js`; introduced unused/quote errors were removed.
- [x] Review complete
