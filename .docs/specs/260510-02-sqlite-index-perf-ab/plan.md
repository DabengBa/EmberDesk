# Plan

- [x] **Done** Task 1: Add SQLite index variant override and route-level measurement metadata
  - Scope:
    - `src/endpoints/character-index.js`
    - `src/endpoints/characters.js`
  - Proof:
    - targeted tests prove `force_off` disables indexed path on the same runtime
    - targeted tests prove measurement mode emits path + duration metadata for `/api/characters/all` and `/api/characters/get`
  - PM Check:
    - normal API payload shape remains unchanged
  - Evidence:
    - Added `EMBERDESK_CHARACTER_INDEX_MODE=auto|force_on|force_off` support in `src/endpoints/character-index.js`
    - Added `EMBERDESK_INTERACTION_PERF_MODE=1` response headers in `src/endpoints/characters.js`
    - Focused proof passed: `cd tests && npm run test:unit -- interaction-performance-index.test.js --runInBand`

- [x] **Done** Task 2: Add repeatable interaction A/B runner and artifact report
  - Scope:
    - `scripts/interaction-performance-runner.mjs`
    - `package.json`
    - any minimal helper/report surface required by the runner
  - Proof:
    - runner completes one local sample and writes `artifacts/interaction-perf/<timestamp>/`
    - report distinguishes first-build and warm-repeat scenarios
  - PM Check:
    - artifacts show which variant path actually executed
  - Evidence:
    - Added `npm run perf:interaction`
    - Added local browser runner at `scripts/interaction-performance-runner.mjs`
    - Runner emits `report.json`, `report.md`, `samples.json`, screenshots, and config snapshot under `artifacts/interaction-perf/<timestamp>/`
    - Focused artifact proof passed:
      - `npm run perf:interaction -- --profile small --scenario characters_all_first_build --pairs 1 --repeats 1`
      - `npm run perf:interaction -- --profile small --scenario characters_all_after_chat_dirty --pairs 1 --repeats 1`
      - `npm run perf:interaction -- --profile small --scenario suite --pairs 1 --repeats 2`

- [x] **Done** Task 3: Update docs for the new measurement workflow
  - Scope:
    - `docs/interaction-performance.md`
    - `.docs/tech/interaction-performance-indexing.md`
    - wrap-up-stage archival notes deferred until feature closeout
  - Proof:
    - `npm run docs:check`
    - `npm run docs:build`
  - Note:
    - `DEVLOG.md` is intentionally deferred to the wrap-up stage so stage detection remains consistent during implementation/review.
